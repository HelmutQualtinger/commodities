version: '3.8'

services:
  commodities:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: commodities-app
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - reverse-proxy

networks:
  reverse-proxy:
    external: true
